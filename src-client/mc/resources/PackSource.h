#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct PackSource {
public:
    // PackSource inner types declare
    // clang-format off
    struct PackTaskData;
    struct RequiredPackOrigin;
    struct RequiredPackType;
    struct RequiredResourceOrBehaviorPackType;
    // clang-format on

    // PackSource inner types define
    struct PackTaskData {};

    struct RequiredPackOrigin {};

    struct RequiredPackType {};

    struct RequiredResourceOrBehaviorPackType {};
};
