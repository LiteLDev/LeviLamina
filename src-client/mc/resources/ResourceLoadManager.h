#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct ResourceLoadManager {
public:
    // ResourceLoadManager inner types declare
    // clang-format off
    struct LoadOrder;
    struct ResourceLoadTaskGroup;
    struct TaskGroupState;
    // clang-format on

    // ResourceLoadManager inner types define
    struct LoadOrder {};

    struct ResourceLoadTaskGroup {};

    struct TaskGroupState {};
};
