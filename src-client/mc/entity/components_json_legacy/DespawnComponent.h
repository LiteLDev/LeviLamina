#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct DespawnComponent {
public:
    // DespawnComponent inner types declare
    // clang-format off
    struct IWorldAccessor;
    struct WorldAccessor;
    // clang-format on

    // DespawnComponent inner types define
    struct IWorldAccessor {};

    struct WorldAccessor {};
};
