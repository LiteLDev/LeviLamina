#pragma once

#include "mc/_HeaderOutputPredefine.h"

namespace bx {

struct Units {
public:
    // Units inner types define
    enum class Enum : int {
        Kilo = 0,
        Kibi = 1,
    };
};

} // namespace bx
