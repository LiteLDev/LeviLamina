#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct DenyList {
public:
    // DenyList inner types declare
    // clang-format off
    struct Entry;
    // clang-format on

    // DenyList inner types define
    enum class Duration : uint {};

    struct Entry {};
};
