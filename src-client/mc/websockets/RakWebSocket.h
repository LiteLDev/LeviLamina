#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct RakWebSocket {
public:
    // RakWebSocket inner types define
    enum class ConnectionState : uint {};

    enum class PacketDirection : uint {};
};
