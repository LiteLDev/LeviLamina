#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct FilePickerSettings {
public:
    // FilePickerSettings inner types declare
    // clang-format off
    struct FileDescription;
    // clang-format on

    // FilePickerSettings inner types define
    enum class PickerType : uint {};

    struct FileDescription {};
};
