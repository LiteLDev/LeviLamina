#pragma once

#include "mc/_HeaderOutputPredefine.h"

namespace glTF {

struct Sampler {
public:
    // Sampler inner types define
    enum class MagFilter : uint {};

    enum class MinFilter : uint {};

    enum class WrapS : uint {};

    enum class WrapT : uint {};
};

} // namespace glTF
