#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct NetworkDebugManager {
public:
    // NetworkDebugManager inner types declare
    // clang-format off
    struct NetherNetStats;
    struct Tracker;
    // clang-format on

    // NetworkDebugManager inner types define
    struct NetherNetStats {};

    struct Tracker {};
};
