#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct WorldTemplateLoader {
public:
    // WorldTemplateLoader inner types define
    enum class Error : uint {};

    enum class InternalState : uint {};

    enum class State : uint {};
};
