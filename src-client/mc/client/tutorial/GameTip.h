#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct GameTip {
public:
    // GameTip inner types declare
    // clang-format off
    struct UIAnchors;
    // clang-format on

    // GameTip inner types define
    enum class ArrowAnchor : uint {};

    enum class ArrowDirection : uint {};

    enum class Status : uint {};

    struct UIAnchors {};
};
