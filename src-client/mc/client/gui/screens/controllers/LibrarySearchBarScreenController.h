#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct LibrarySearchBarScreenController {
public:
    // LibrarySearchBarScreenController inner types declare
    // clang-format off
    struct SearchConfig;
    // clang-format on

    // LibrarySearchBarScreenController inner types define
    struct SearchConfig {};
};
