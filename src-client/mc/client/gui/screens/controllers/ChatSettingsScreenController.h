#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct ChatSettingsScreenController {
public:
    // ChatSettingsScreenController inner types declare
    // clang-format off
    struct ChatSettings;
    // clang-format on

    // ChatSettingsScreenController inner types define
    enum class ColorSubsection : uint {};

    struct ChatSettings {};
};
