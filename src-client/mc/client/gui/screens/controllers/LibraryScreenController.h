#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct LibraryScreenController {
public:
    // LibraryScreenController inner types define
    enum class ErrorType : uint {};

    enum class ItemType : uint {};

    enum class OptionalFeature : uint {};

    enum class ScreenType : uint {};
};
