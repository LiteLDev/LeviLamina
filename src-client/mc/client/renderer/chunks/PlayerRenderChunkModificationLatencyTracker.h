#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct PlayerRenderChunkModificationLatencyTracker {
public:
    // PlayerRenderChunkModificationLatencyTracker inner types declare
    // clang-format off
    struct TimeDelta;
    struct TimeMarker;
    // clang-format on

    // PlayerRenderChunkModificationLatencyTracker inner types define
    struct TimeDelta {};

    struct TimeMarker {};
};
