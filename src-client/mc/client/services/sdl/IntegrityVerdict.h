#pragma once

#include "mc/_HeaderOutputPredefine.h"

namespace SDL {

struct IntegrityVerdict {
public:
    // IntegrityVerdict inner types define
    enum class AppVerdict : uint {};

    enum class DeviceVerdict : uint {};

    enum class LicenseVerdict : uint {};
};

} // namespace SDL
