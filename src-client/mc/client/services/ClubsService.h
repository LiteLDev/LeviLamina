#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct ClubsService {
public:
    // ClubsService inner types declare
    // clang-format off
    struct RequestInfo;
    // clang-format on

    // ClubsService inner types define
    struct RequestInfo {
    public:
        // RequestInfo inner types define
        enum class Status : int {};
    };
};
