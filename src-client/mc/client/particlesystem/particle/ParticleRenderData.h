#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct ParticleRenderData {
public:
    // ParticleRenderData inner types declare
    // clang-format off
    struct ParticleData;
    // clang-format on

    // ParticleRenderData inner types define
    enum class FaceCameraMode : uint {};

    struct ParticleData {};
};
