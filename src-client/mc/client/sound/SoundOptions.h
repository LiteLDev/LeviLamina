#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct SoundOptions {
public:
    // SoundOptions inner types declare
    // clang-format off
    struct VolumeSlider;
    // clang-format on

    // SoundOptions inner types define
    struct VolumeSlider {};
};
