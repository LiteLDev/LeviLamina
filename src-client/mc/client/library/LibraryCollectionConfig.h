#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct LibraryCollectionConfig {
public:
    // LibraryCollectionConfig inner types define
    enum class CollectionSource : uint {};

    enum class ExcludedDisplayCategory : uint {};
};
