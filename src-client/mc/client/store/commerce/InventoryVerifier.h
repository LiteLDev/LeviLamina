#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct InventoryVerifier {
public:
    // InventoryVerifier inner types declare
    // clang-format off
    struct ReceiptWithMap;
    // clang-format on

    // InventoryVerifier inner types define
    struct ReceiptWithMap {};
};
