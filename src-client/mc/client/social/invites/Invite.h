#pragma once

#include "mc/_HeaderOutputPredefine.h"

namespace Invites {

struct Invite {
public:
    // Invite inner types define
    enum class InvitationStatus : uint {};

    enum class InvitationType : uint {};

    enum class State : uint {};
};

} // namespace Invites
