#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct LinkedAssetValidator {
public:
    // LinkedAssetValidator inner types declare
    // clang-format off
    struct AssetCollection;
    struct KnownObject;
    struct KnownObjectHasher;
    struct Reference;
    // clang-format on

    // LinkedAssetValidator inner types define
    enum class ValidatorStatus : uint {};

    struct AssetCollection {};

    struct KnownObject {};

    struct KnownObjectHasher {};

    struct Reference {};
};
