#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct HashType64_hash {
public:
    // prevent constructor by default
    HashType64_hash& operator=(HashType64_hash const&);
    HashType64_hash(HashType64_hash const&);
    HashType64_hash();
};
