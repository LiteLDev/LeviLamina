#pragma once

#include "mc/_HeaderOutputPredefine.h"

class PackDownloadManager {
public:
    // prevent constructor by default
    PackDownloadManager& operator=(PackDownloadManager const&);
    PackDownloadManager(PackDownloadManager const&);
    PackDownloadManager();
};
