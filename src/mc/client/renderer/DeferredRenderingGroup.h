#pragma once

#include "mc/_HeaderOutputPredefine.h"

class DeferredRenderingGroup {
public:
    // DeferredRenderingGroup inner types declare
    // clang-format off
    struct DeferredRenderingSettings;
    // clang-format on

    // DeferredRenderingGroup inner types define
    struct DeferredRenderingSettings {};
};
