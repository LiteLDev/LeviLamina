#pragma once

#include "mc/_HeaderOutputPredefine.h"

class VoiceSystem {
public:
    // prevent constructor by default
    VoiceSystem& operator=(VoiceSystem const&);
    VoiceSystem(VoiceSystem const&);
    VoiceSystem();
};
