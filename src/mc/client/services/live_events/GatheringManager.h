#pragma once

#include "mc/_HeaderOutputPredefine.h"

class GatheringManager {
public:
    // prevent constructor by default
    GatheringManager& operator=(GatheringManager const&);
    GatheringManager(GatheringManager const&);
    GatheringManager();
};
