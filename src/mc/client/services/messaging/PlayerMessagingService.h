#pragma once

#include "mc/_HeaderOutputPredefine.h"

class PlayerMessagingService {
public:
    // prevent constructor by default
    PlayerMessagingService& operator=(PlayerMessagingService const&);
    PlayerMessagingService(PlayerMessagingService const&);
    PlayerMessagingService();
};
