#pragma once

#include "mc/_HeaderOutputPredefine.h"

namespace OreUI {

class Router {
public:
    // prevent constructor by default
    Router& operator=(Router const&);
    Router(Router const&);
    Router();
};

} // namespace OreUI
