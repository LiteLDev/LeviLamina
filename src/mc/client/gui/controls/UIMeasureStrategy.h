#pragma once

#include "mc/_HeaderOutputPredefine.h"

class UIMeasureStrategy {
public:
    // prevent constructor by default
    UIMeasureStrategy& operator=(UIMeasureStrategy const&);
    UIMeasureStrategy(UIMeasureStrategy const&);
    UIMeasureStrategy();
};
