#pragma once

#include "mc/_HeaderOutputPredefine.h"

template <typename T0>
class NetworkedBlockComponentDescription {
public:
    // prevent constructor by default
    NetworkedBlockComponentDescription& operator=(NetworkedBlockComponentDescription const&);
    NetworkedBlockComponentDescription(NetworkedBlockComponentDescription const&);
    NetworkedBlockComponentDescription();
};
