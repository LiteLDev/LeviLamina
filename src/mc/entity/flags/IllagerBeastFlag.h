#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct IllagerBeastFlag {
public:
    // prevent constructor by default
    IllagerBeastFlag& operator=(IllagerBeastFlag const&);
    IllagerBeastFlag(IllagerBeastFlag const&);
    IllagerBeastFlag();
};
