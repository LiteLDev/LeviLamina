#pragma once

#include "mc/_HeaderOutputPredefine.h"

namespace OreUI {

struct LevelDataBindings {
public:
    // LevelDataBindings inner types declare
    // clang-format off
    struct Advanced;
    struct AdvancedOreUI;
    struct Advancedstruct;
    struct Cheats;
    struct CheatsOreUI;
    struct Cheatsstruct;
    struct General;
    struct GeneralOreUI;
    struct Generalstruct;
    struct Multiplayer;
    struct MultiplayerOreUI;
    struct Multiplayerstruct;
    struct ResourcePacks;
    struct ResourcePacksOreUI;
    struct ResourcePacksstruct;
    // clang-format on

    // LevelDataBindings inner types define
    struct Advanced {};

    struct AdvancedOreUI {
    public:
        // AdvancedOreUI inner types declare
        // clang-format off
        struct LevelDataBindings;
        // clang-format on

        // AdvancedOreUI inner types define
        struct LevelDataBindings {};
    };

    struct Advancedstruct {};

    struct Cheats {};

    struct CheatsOreUI {
    public:
        // CheatsOreUI inner types declare
        // clang-format off
        struct LevelDataBindings;
        // clang-format on

        // CheatsOreUI inner types define
        struct LevelDataBindings {};
    };

    struct Cheatsstruct {};

    struct General {};

    struct GeneralOreUI {
    public:
        // GeneralOreUI inner types declare
        // clang-format off
        struct LevelDataBindings;
        // clang-format on

        // GeneralOreUI inner types define
        struct LevelDataBindings {};
    };

    struct Generalstruct {};

    struct Multiplayer {};

    struct MultiplayerOreUI {
    public:
        // MultiplayerOreUI inner types declare
        // clang-format off
        struct LevelDataBindings;
        // clang-format on

        // MultiplayerOreUI inner types define
        struct LevelDataBindings {};
    };

    struct Multiplayerstruct {};

    struct ResourcePacks {};

    struct ResourcePacksOreUI {
    public:
        // ResourcePacksOreUI inner types declare
        // clang-format off
        struct LevelDataBindings;
        // clang-format on

        // ResourcePacksOreUI inner types define
        struct LevelDataBindings {};
    };

    struct ResourcePacksstruct {};
};

} // namespace OreUI
