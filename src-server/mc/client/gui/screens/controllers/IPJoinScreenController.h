#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct IPJoinScreenController {
public:
    // IPJoinScreenController inner types declare
    // clang-format off
    struct JoinData;
    // clang-format on

    // IPJoinScreenController inner types define
    enum class JoinRequest : uchar {};

    struct JoinData {};
};
