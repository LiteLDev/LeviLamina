#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct EcoModeSystem {
public:
    // EcoModeSystem inner types declare
    // clang-format off
    struct EcoModeClientState;
    struct ScreenConfiguration;
    // clang-format on

    // EcoModeSystem inner types define
    struct EcoModeClientState {};

    struct ScreenConfiguration {};
};
