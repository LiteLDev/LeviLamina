#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct MapRenderer {
public:
    // MapRenderer inner types declare
    // clang-format off
    struct MapInstance;
    // clang-format on

    // MapRenderer inner types define
    struct MapInstance {};
};
