#pragma once

#include "mc/_HeaderOutputPredefine.h"

struct GameTip {
public:
    // GameTip inner types declare
    // clang-format off
    struct UIAnchors;
    // clang-format on

    // GameTip inner types define
    enum class ArrowAnchor : int {};

    enum class ArrowDirection : int {};

    enum class Status : int {};

    struct UIAnchors {};
};
